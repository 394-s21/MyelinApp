import React from 'react'
import { render, fireEvent } from '@testing-library/react-native';

import Task from '../components/Task';


// Render test.
test('screen renders', () => {
    expect(1 + 2).toBe(3);
    //render (<Task task={tasks} view={view}/>);
});

// Happy path test.


// Unhappy path test.